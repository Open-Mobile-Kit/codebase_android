package com.open_mobile_kit.android.codebase.application

/**
 * Represents a single, specific user intention or system action.
 *
 * Commands are simple, immutable data objects that are sent to an [IService]
 * to trigger a business operation.
 *
 * It is recommended to implement this as a data class.
 */
interface ICommand
